# 🧠 Project Memory (`memory.md`)

## 1. Snapshot
- **Current Phase:** Phase 7 — Academic Calendar Hub & Weekly Timetable (✅ 100% Complete & Verified across all tests)
- **What's Working Right Now:**
  - **Full Full-Stack Foundation (Phase 0 & 1):** Railway PostgreSQL database connected via Prisma ORM, Upstash Redis caching layer connected, Express REST API with RBAC JWT authentication (HTTP-only cookies, `validate`, `auth`, `admin` middlewares), and React Vite client with Tailwind CSS v3 & shadcn/ui base components (`Card`, `Button`, `Input`, `Label`).
  - **Core Academic Tracking (Phase 2):** Subject attendance logging with live 75.0% compliance threshold (`AttendanceBadge.jsx`, `Attendance.jsx`), semester grade sheet management (`Grades.jsx`, `GradeRow.jsx`), and university standard 10-point scale SGPA/CGPA calculation engine (`gradeEngine.js`, `SGPADisplay.jsx`, `SGPALineChart.jsx`). Verified via `phase2.verify.js`.
  - **Task & Assignment Scheduler (Phase 3):** Full academic task management (`Tasks.jsx`) supporting multi-view layouts: Kanban Board (`KanbanBoard.jsx`), Tabular List (`TaskList.jsx`), and Monthly Calendar (`CalendarView.jsx`). Supports priority (`HIGH/MEDIUM/LOW`), category (`ASSIGNMENT/EXAM/PROJECT/PERSONAL`), and subject linkage (`tasks.controller.js`). Verified via `phase3.verify.js`.
  - **CGPA Predictor + Study Resources & Notes (Phase 4):** Interactive target SGPA/CGPA simulation engine (`Predictor.jsx`, `PredictionChart.jsx`, `gradeEngine.js`), personal student markdown study notes with categorization (`Resources.jsx`, `NoteCard.jsx`), and course study links with admin pinned (`isPinned: true`) badges (`LinkCard.jsx`). Verified via `phase4.verify.js`.
  - **Admin Tools & Monitoring (Phase 5):** Complete admin control panel (`AdminDashboard.jsx`, `CourseManager.jsx`, `StudentDirectory.jsx`, `StudentProfile.jsx`, `AdminNavbar.jsx`) with platform analytics, collapsible course tree management, searchable student monitoring, and strict student note/link privacy isolation. Verified via `phase5.verify.js`.
  - **Visual System & UI Polish (Phase 6 per `design.md`):** High contrast tokens, component anatomy, ledger strips, and responsive bento grids across all screens.
  - **Academic Calendar Hub & Weekly Timetable (Phase 7):**
    - **Weekly Timetable (Schedule Setup):** `ClassSchedule` Prisma model mapping (`studentId`, `subjectId`, `semesterNo`, `dayOfWeek`). Strictly isolated by student's current semester (`GET /api/v1/schedule/me?semesterNo=`, `POST /api/v1/schedule`). If unscheduled, falls back cleanly to treating all 7 days of the week as valid class days (`[0, 1, 2, 3, 4, 5, 6]`). `ScheduleSetupModal.jsx` in `Attendance.jsx` allows quick day picking per subject. Verified via `node phase7.schedule.verify.js` (`100% SUCCESS`).
    - **Academic Calendar Hub (3-Tier Scope):** `AcademicEvent` Prisma model supporting `EXAM`, `DEADLINE`, `HOLIDAY`, `OTHER`. Scoped via `courseId` & `semesterNo` forming three tiers: Global (`null`/`null`), Course-Wide (`courseId`/`null`), and Semester-Scoped (`courseId`/`semesterNo`). Admin CRUD at `/admin/academic-calendar` (`AdminAcademicCalendar.jsx`). Student query (`GET /api/v1/academic-events`) strictly filtered server-side based on `req.user.courseId` and `req.user.currentSemester` while selecting only public fields (`createdById` stripped to prevent leakage). Events render as `status-info` chips inside `CalendarView.jsx` (`Tasks.jsx`) with a detail popover and do not count toward personal "Tasks Due Soon". Verified via `node phase7.events.verify.js` (`100% SUCCESS`).
- **What's Half-Done:** None. All phases (Phases 0–7) are functionally and visually complete to spec.

## 2. Decisions Log
- **2026-07-14:** Confirmed project root is `/Users/anirudhbhatia/acadtracker`. Established `memory.md` per instructions before starting Phase 0 setup. Confirmed strict adherence to cloud-first structure (Railway PostgreSQL, Upstash Redis) and exact formula/threshold constraints (75% attendance fixed, 10-point scale for CGPA).
- **2026-07-14:** Used Tailwind CSS v3.4.17 in `/client` to ensure 100% compatibility with `design.md` Section 12 `tailwind.config.js` and `shadcn/ui` base layer styling.
- **2026-07-15:** Configured local `/server/.env` with Railway PostgreSQL TCP Proxy URL (`turntable.proxy.rlwy.net:13524`) for local schema migrations while keeping `postgres.railway.internal:5432` for production Railway deployment.
- **2026-07-15:** Standardized `responseHelper.js` parameter signatures (`sendSuccess(res, data, message, statusCode)` and `sendError(res, message, statusCode, errorCode, details)`) across all middlewares and controllers.
- **2026-07-15:** Implemented automatic login inside `register` action in `authStore.js` (`POST /auth/register` followed by `POST /auth/login`) so newly registered students land directly on `/onboarding`.
- **2026-07-15 (Phase 2 & 4):** Standardized 10-point grade mapping (`O: 10, A_PLUS: 9, A: 8, B_PLUS: 7, B: 6, C: 5, D: 4, F: 0`) inside `gradeMap.js` and `gradeEngine.js` for consistent calculation across `grades.controller.js`, `predictor.controller.js`, and `admin.controller.js`.
- **2026-07-15 (Phase 5):** Resolved `EADDRINUSE: address already in use :::5000` macOS AirPlay conflict by permanently updating `server/.env` and `client/src/services/api.js` to **PORT 5001**.
- **2026-07-15 (Phase 5):** Enforced strict data privacy in `admin.controller.js` (`getStudentProfile`). Admin queries return student user info, attendance logs, grades, tasks, and course-pinned resources (`isPinned: true`), but explicitly exclude personal `Note` records and unpinned `Resource` records created by students.
- **2026-07-15 (Phase 6 Visual Pass):** Mapped `design.md` §3.1 & §10 CSS variables directly to shadcn/ui and Tailwind tokens (`background`, `foreground`, `card`, `popover`, `border`, `muted`). Updated all student and admin views to eliminate hardcoded hex/slate colors, enforce icon+text `Badge` statuses, and integrate `AttendanceLedgerStrip`.
- **2026-07-16 (Grade Map Official Scale Update):** Updated `gradeMap.js` (`GRADE_POINTS`), `schema.prisma` (`enum LetterGrade` adding `E`), `GradeRow.jsx` (`LETTER_GRADES` points labels), and `gradeEngine.js` (`calculateBestCase` and `calculateWorstCase`). Verified via `node server/phase2.verify.js` (`177.5 / 21 = 8.45` exact) and `node server/phase4.verify.js`.
- **2026-07-17 (Attendance Calendar Modal, Date Parity Fix, & Single-Day Click Range-Fill Fix):** Added overlay calendar modal to `Attendance.jsx` via `CalendarView.jsx` (`mode="attendance"`). Resolved date parity bug (`* 2` demo spacing increment) inside `attendanceUtils.js` (`generateSessions`) so ledger strip and calendar match 1-for-1 on consecutive dates. Resolved single-day click range-fill bug (`handleSetStatus` in `CalendarView.jsx`) by capturing and preserving exact daily `records` (`existingRecords` / `nextRecords`), updating only the single clicked date (`or inserting/removing exactly one record`), and starting `upcomingList` dates 1 day after the latest held class (`lastHeldDate`). Verified clean via `npm run build`, `node phase2.verify.js`, and `node phase3.verify.js` (`100% SUCCESS`).
- **2026-07-20 (Phase 7 - Timetable & Academic Calendar Hub):**
  - **Semester-Scoped Timetable:** Enforced strict `semesterNo` filtering when saving/querying `ClassSchedule` (`rules.md §9.1`), fixing the multi-semester leak issue where every subject across every semester appeared. If `ClassSchedule` rows count is 0, frontend/backend fallback returns `[0, 1, 2, 3, 4, 5, 6]` (all 7 days).
  - **Academic Calendar Hub Scoping & Leak Prevention:** Created `AcademicEvent` (`rules.md §9.2`) supporting nullable `courseId` & `semesterNo` for 3-tier visibility (`Global`, `Course-Wide`, `Semester-Scoped`). Enforced strict server-side filtering in `getStudentEvents` (`req.user.courseId` & `req.user.currentSemester`) and explicitly applied Prisma `select` block to exclude `createdById` and admin metadata from student payloads. Integrated events as read-only `status-info` chips inside `CalendarView.jsx` (`Tasks.jsx`), excluded from personal "Tasks Due Soon" metrics.
  - **Admin Events & Stacked Toast Diagnosis (`AdminAcademicCalendar.jsx`):** Diagnosed and fixed the root causes of the duplicate `"Failed to load academic events"` error toast when loading `/admin/academic-calendar`:
    1. **Dedicated Endpoint Verification:** Verified via direct HTTP testing (`GET /api/v1/admin/academic-events`) that admin management has its own dedicated endpoint returning all events unfiltered (`200 OK`), separate from `GET /api/v1/academic-events` (`student-facing server-side scoped`).
    2. **Method Signature & Data Shape Fix:** `fetchData()` in `AdminAcademicCalendar.jsx` called `adminService.getCourses()` (`which threw a TypeError because adminService exports getAllCourses`), and accessed `eventsRes?.events` instead of `eventsRes?.data?.events` (`matching API success helper payload shape`). Corrected both calls.
    3. **Toast Deduplication (`React 18 StrictMode`):** Because `React.StrictMode` double-mounts components in development (`main.jsx`), `useEffect` triggered two parallel `fetchData()` executions. Added `toast.dismiss()` guards and explicit IDs (`{ id: 'admin-events-error' }`, `{ id: 'event-form-error' }`, `{ id: 'event-action' }`) across fetch, create, update, and delete actions so duplicate parallel throws never stack toasts.
  - **Full Verification:** Verified zero regressions across the codebase with `node phase7.schedule.verify.js` (`100% SUCCESS`) and `node phase7.events.verify.js` (`100% SUCCESS`), plus `npm run build`.

## 3. Changes Log
- **2026-07-14 to 2026-07-17:** Phases 0 through 6 completed, audited, and verified (Auth, Core Academic, Task Scheduler, Predictor, Resources, Admin Tools, and UI Polish per `design.md`).
- **2026-07-18 (Feature 1 - Weekly Timetable):** Added `ClassSchedule` model to `schema.prisma`. Built `schedule.controller.js`, `schedule.routes.js`, `schedule.service.js`, and `schedule.schema.js` (`Zod`). Created `ScheduleSetupModal.jsx` and integrated schedule-aware attendance session generation (`attendanceUtils.js`). Verified via `node phase7.schedule.verify.js` (`100% PASS`).
- **2026-07-20 (Feature 2 - Academic Calendar Hub & Admin Toast Fix):** Added `AcademicEvent` model and `EventType` enum (`EXAM`, `DEADLINE`, `HOLIDAY`, `OTHER`) to `schema.prisma`. Built `academic-event.controller.js`, `academic-event.routes.js`, `academic-event.service.js` (with explicit `select` excluding `createdById`), and `academic-event.schema.js`. Created `AdminAcademicCalendar.jsx` (`/admin/academic-calendar`), updated `AdminNavbar.jsx`, `App.jsx`, `CalendarView.jsx`, and `Tasks.jsx`. Fixed `AdminAcademicCalendar.jsx` method call (`adminService.getAllCourses()`), payload extraction (`eventsRes.data.events`), and added toast deduplication (`id` / `dismiss`). Updated documentation (`architecture.md`, `rules.md`, `PRD_Student_Academic_Platform.md`, `design.md`, `phases.md`, `memory.md`). Verified via `node phase7.events.verify.js`, `node phase7.schedule.verify.js`, and `npm run build` (`100% PASS`).

## 4. Open Questions / Known Issues
- None. All functional requirements and scoping constraints for both Weekly Timetable and Academic Calendar Hub are fully verified and clean.
