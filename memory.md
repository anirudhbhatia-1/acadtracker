# 🧠 Project Memory (`memory.md`)

## 1. Snapshot
- **Current Phase:** Phase 6 — Visual Pass & UI Polish (✅ 100% Complete & Verified across all Student & Admin screens)
- **What's Working Right Now:**
  - **Full Full-Stack Foundation (Phase 0 & 1):** Railway PostgreSQL database connected via Prisma ORM, Upstash Redis caching layer connected, Express REST API with RBAC JWT authentication (HTTP-only cookies, `validate`, `auth`, `admin` middlewares), and React Vite client with Tailwind CSS v3 & shadcn/ui base components (`Card`, `Button`, `Input`, `Label`).
  - **Core Academic Tracking (Phase 2):** Subject attendance logging with live 75.0% compliance threshold (`AttendanceBadge.jsx`, `Attendance.jsx`), semester grade sheet management (`Grades.jsx`, `GradeRow.jsx`), and university standard 10-point scale SGPA/CGPA calculation engine (`gradeEngine.js`, `SGPADisplay.jsx`, `SGPALineChart.jsx`). Verified via `phase2.verify.js`.
  - **Task & Assignment Scheduler (Phase 3):** Full academic task management (`Tasks.jsx`) supporting multi-view layouts: Kanban Board (`KanbanBoard.jsx`), Tabular List (`TaskList.jsx`), and Monthly Calendar (`CalendarView.jsx`). Supports priority (`HIGH/MEDIUM/LOW`), category (`ASSIGNMENT/EXAM/PROJECT/PERSONAL`), and subject linkage (`tasks.controller.js`). Verified via `phase3.verify.js`.
  - **CGPA Predictor + Study Resources & Notes (Phase 4):** Interactive target SGPA/CGPA simulation engine (`Predictor.jsx`, `PredictionChart.jsx`, `gradeEngine.js`), personal student markdown study notes with categorization (`Resources.jsx`, `NoteCard.jsx`), and course study links with admin pinned (`isPinned: true`) badges (`LinkCard.jsx`). Verified via `phase4.verify.js`.
  - **Admin Tools & Monitoring (Phase 5):** Complete admin control panel (`AdminDashboard.jsx`, `CourseManager.jsx`, `StudentDirectory.jsx`, `StudentProfile.jsx`, `AdminNavbar.jsx`) with platform analytics, collapsible course tree management, searchable student monitoring, and strict student note/link privacy isolation. Verified via `phase5.verify.js`.
  - **Visual System & UI Polish (Phase 6 per `design.md`):**
    - **Design Tokens & High Contrast (§3.1, §9, §10):** Loaded `Fraunces`, `Inter`, and `IBM Plex Mono`. Mapped semantic tokens (`--bg`, `--surface`, `--surface-2`, `--border`, `--text`, `--text-muted`, `--ink`, `--chalk-teal`) across `index.css` and `tailwind.config.js`. Guaranteed WCAG 2.1 AA 4.5:1+ contrast across light (`#F8F6F0` / `#FFFFFF`) and dark (`#0C0D0E` / `#16181A`) modes, resolving near-black-on-near-black unreadability.
    - **App Shell (§5):** `StudentLayout.jsx` with persistent sidebar (240px desktop, auto-collapsing drawer on mobile), bottom profile card (`TopNavbar.jsx` profile section), and active route highlight (`border-l-2` / `shadow-xs`).
    - **Component Anatomy & Status Indicators (§6):** Restyled `Card` / `CardHeader` / `CardTitle` / `CardLabel` (`Card.jsx`), `Button` (`button.jsx`), `Input` (`input.jsx`), `Table` (`table.jsx`), `Badge` (`badge.jsx`), and `.seg` segmented tabs. Ensured **every status indicator uses icon + text (`Badge` with `lucide-react` icons), never color alone**.
    - **Attendance Ledger Strip (§4 & §7.4):** Integrated `AttendanceLedgerStrip.jsx` with real-time progress bar (`safe`/`warning`/`critical` thresholds), 28-session square visual array, and quick log buttons (`+ Attended`, `+ Missed`) into `Attendance.jsx` and `StudentDashboard.jsx`.
    - **Complete Screen-by-Screen Restyling (§7 & §8):** Restyled every page and modal (`StudentDashboard`, `Grades`, `Tasks`, `TaskModal`, `Predictor`, `Resources`, `NoteCard`, `LinkCard`, `CourseSelection`, `Login`, `Register`, `AdminNavbar`, `AdminDashboard`, `CourseManager`, `StudentDirectory`, `StudentProfile`). All screens verified via `vite build`.
- **What's Half-Done:** None. All phases (Phases 0–6) are functionally and visually complete to spec.

## 2. Decisions Log
- **2026-07-14:** Confirmed project root is `/Users/anirudhbhatia/acadtracker`. Established `memory.md` per instructions before starting Phase 0 setup. Confirmed strict adherence to cloud-first structure (Railway PostgreSQL, Upstash Redis) and exact formula/threshold constraints (75% attendance fixed, 10-point scale for CGPA).
- **2026-07-14:** Used Tailwind CSS v3.4.17 in `/client` to ensure 100% compatibility with `design.md` Section 12 `tailwind.config.js` and `shadcn/ui` base layer styling.
- **2026-07-15:** Configured local `/server/.env` with Railway PostgreSQL TCP Proxy URL (`turntable.proxy.rlwy.net:13524`) for local schema migrations while keeping `postgres.railway.internal:5432` for production Railway deployment.
- **2026-07-15:** Standardized `responseHelper.js` parameter signatures (`sendSuccess(res, data, message, statusCode)` and `sendError(res, message, statusCode, errorCode, details)`) across all middlewares and controllers.
- **2026-07-15:** Implemented automatic login inside `register` action in `authStore.js` (`POST /auth/register` followed by `POST /auth/login`) so newly registered students land directly on `/onboarding`.
- **2026-07-15 (Phase 2 & 4):** Standardized 10-point grade mapping (`O: 10, A_PLUS: 9, A: 8, B_PLUS: 7, B: 6, C: 5, D: 4, F: 0`) inside `gradeMap.js` and `gradeEngine.js` for consistent calculation across `grades.controller.js`, `predictor.controller.js`, and `admin.controller.js`.
- **2026-07-15 (Phase 5):** Resolved `EADDRINUSE: address already in use :::5000` macOS AirPlay conflict by permanently updating `server/.env` and `client/src/services/api.js` to **PORT 5001**.
- **2026-07-15 (Phase 5):** Enforced strict data privacy in `admin.controller.js` (`getStudentProfile`). Admin queries return student user info, attendance logs, grades, tasks, and course-pinned resources (`isPinned: true`), but explicitly exclude personal `Note` records and unpinned `Resource` records created by students.
- **2026-07-15 (Phase 6 Visual Pass):** Mapped `design.md` §3.1 & §10 CSS variables directly to shadcn/ui and Tailwind tokens (`background`, `foreground`, `card`, `popover`, `border`, `muted`). Updated all student and admin views to eliminate hardcoded hex/slate colors, enforce icon+text `Badge` statuses, and integrate `AttendanceLedgerStrip`.
- **2026-07-15 (Audit & Bug Fix - Tasks):** Discovered and fixed Zod validation error on `POST /api/v1/tasks` when adding a task from the UI without a description. `TaskModal.jsx` sends `description: null` when left blank, but `createTaskSchema` (`tasks.schema.js:10`) and `broadcastTaskSchema` (`tasks.schema.js:33`) were missing `.nullable()`. Added `.nullable()` to match `updateTaskSchema` (`tasks.schema.js:21`) and `schema.prisma`.
- **2026-07-15 (Predictor Bug Fix & UX Pass):** Resolved all 6 Predictor bugs:
  1. Added `sgpa: z.number().min(0).max(10).optional()` to `simulateSchema` (`predictor.schema.js`) to prevent Zod from stripping incoming slider SGPA values (`futureSemesters.sgpa`).
  2. Fixed independent slider state (`futureSemestersInput`) in `Predictor.jsx` so each remaining semester retains and moves its slider independently without resetting or clamping to `8.0`.
  3. Fixed `PredictionChart.jsx` X-axis label bug (`"Sem undefined"`) by reading `(item.semesterNo || item.semester)` and updated `predictCGPA` in `gradeEngine.js` to always populate `semesterNo`.
  4. Restored distinct solid vs. dashed line styling (`Actual` completed semesters vs. `Predicted` future semesters) across `PredictionChart.jsx` and `gradeEngine.js`.
  5. Removed competing/overlapping `"Calculate"` and `"Apply Simulation"` buttons from `Predictor.jsx`, unifying the action into a single atomic `"Run Simulation"` flow with `toast.dismiss('predictor-error')` guards to eliminate toast spam.
  6. Behaviorally verified mathematical bounds (`10pt` max / `4pt` min remaining credit formulas) and `minSGPANeeded` against hand calculations and `phase4.verify.js`.

- **2026-07-15 (Attendance Bug Fix & UX Pass):** Resolved all single-tap, batch logging, and Undo affordance requirements:
  1. **Fixed Zod Validation Crash on Single-Tap:** Tapping `+ Attended` or `+ Missed` in `Attendance.jsx` crashed because `handleQuickLog(subjectId, status)` called `logAttendance(subjectId, status)`. The missing `semesterNo` argument caused `action` and `semesterNo` to shift to `"ATTENDED"`, triggering a Zod `400 Bad Request` (`Expected number, received string`). Fixed by explicitly passing `Number(selectedSemester)`.
  2. **Single-Tap Logging & No Present Default:** Retained clean single-tap `+ Attended` and `+ Missed` buttons on `AttendanceLedgerStrip.jsx` without any pre-defaulting modal or "Present" default selection.
  3. **Multi-Session / Batch Logging:** Added `onLogBatch` and `+ Batch` popover to `AttendanceLedgerStrip.jsx` allowing quick selection (`+2 Attended`, `+3 Attended`, `+2 Missed`) for lab sessions and multiple lectures.
  4. **Undo Affordance & Instant UI Updates:** Updated `logAttendance` in `academicStore.js` to immediately update local Zustand state (`attendance`) without full page reloads, and show a custom interactive toast with an **Undo** button. Clicking Undo reverts state to `oldTotal` and `oldAttended` via `PATCH /api/v1/attendance/:id`.

## 3. Changes Log
- **2026-07-14:** Initialized `memory.md`, created root `.gitignore`, initialized Git repository (`main` and `dev` branches). Scaffolded `/client` (React + Vite, Tailwind CSS v3, shadcn/ui) and `/server` (Express, Prisma ORM, Upstash Redis).
- **2026-07-15:** Completed Phase 0 and Phase 1 (Auth & Onboarding Backend + Frontend). Wrote `prisma/schema.prisma` (8 models, 8 enums) and `seed.js`. Verified build and API test suites.
- **2026-07-15 (Phase 2):** Built core academic module (`attendance.controller.js`, `grades.controller.js`, `gradeEngine.js`, `Attendance.jsx`, `Grades.jsx`, `SGPADisplay.jsx`, `SGPALineChart.jsx`). Verified via `node phase2.verify.js`.
- **2026-07-15 (Phase 3):** Built task & assignment scheduler (`tasks.controller.js`, `KanbanBoard.jsx`, `TaskList.jsx`, `CalendarView.jsx`, `Tasks.jsx`). Verified via `node phase3.verify.js`.
- **2026-07-15 (Phase 4):** Built CGPA predictor, notes, and study resources (`predictor.controller.js`, `notes.controller.js`, `resources.controller.js`, `Predictor.jsx`, `Resources.jsx`, `NoteCard.jsx`, `LinkCard.jsx`). Verified via `node phase4.verify.js`.
- **2026-07-15 (Phase 5):** Built admin control panel and analytics (`admin.controller.js`, `AdminDashboard.jsx`, `CourseManager.jsx`, `StudentDirectory.jsx`, `StudentProfile.jsx`). Verified backend via `phase5.verify.js` and client via `npm run build`.
- **2026-07-15 (Phase 6 Visual Pass):** Complete UI rebuild matching `design.md` §3–§10 without touching any API, route, store, or Prisma logic. Restyled `index.html`, `tailwind.config.js`, `index.css`, UI primitives (`Card`, `Button`, `Input`, `Table`, `Badge`), `AttendanceLedgerStrip`, student layout (`StudentLayout`, `TopNavbar`), and every student (`StudentDashboard`, `Grades`, `Tasks`, `Predictor`, `Resources`, `CourseSelection`, `Attendance`, `Login`, `Register`) and admin page (`AdminDashboard`, `CourseManager`, `StudentDirectory`, `StudentProfile`, `AdminNavbar`).
- **2026-07-15 (Audit & Bug Fix - Tasks):** Updated `server/src/modules/tasks/tasks.schema.js` (`createTaskSchema` and `broadcastTaskSchema`) to chain `.nullable()` to `description: z.string().optional()`, resolving `400 Bad Request` (`Expected string, received null`) when adding tasks from the frontend.
- **2026-07-15 (Predictor Bug Fix & UX Pass):** Updated `predictor.schema.js`, `gradeEngine.js`, `Predictor.jsx`, and `PredictionChart.jsx`. Resolved toast spam (`toast.dismiss()`), fixed Zod stripping `sgpa` from slider simulation inputs, corrected chart `Sem undefined` X-axis labels, enforced Actual vs Predicted line split, and unified simulation buttons into a single `"Run Simulation"` trigger. Verified clean via `node phase4.verify.js` and `vite build`.
- **2026-07-15 (Attendance Bug Fix & UX Pass):** Updated `Attendance.jsx`, `AttendanceLedgerStrip.jsx`, and `academicStore.js`. Fixed Zod parameter crash (`Expected number, received string`), added multi-session batch logging dropdown (`+ Batch`), enforced explicit single-tap logging (`+ Attended` / `+ Missed`), and added an instant UI update with an interactive **Undo** toast (`PATCH /api/v1/attendance/:id`). Verified clean via `node phase2.verify.js`, `node phase4.verify.js`, and `vite build`.

## 4. Open Questions / Known Issues
- None. All functional requirements and UX improvements for CGPA Predictor and Attendance are fully verified against running backend tests and production builds.
